__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
*.log

# Local credentials for diagnostics against a real grill, never commit.
.env

# Extractor output — contains app credentials, never commit.
ninja_woodfire_credentials.txt
*.creds.txt

# Deploy target + long-lived HA token, never commit.
.env.deploy

# mise-managed virtualenv
.venv/

# Output of scripts/ble_probe.py — contains device identifiers.
ble_probe_output.json

# Local card preview staging, not part of the integration.
.preview/
